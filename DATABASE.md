# 🗄️ Руководство по подключению к SQL Server БД

Полное руководство по использованию SQL Server в качестве источника данных.

## 📋 Содержание

- [Требования](#требования)
- [Установка зависимостей](#установка-зависимостей)
- [Настройка подключения](#настройка-подключения)
- [Использование](#использование)
- [Структура БД](#структура-бд)
- [Решение проблем](#решение-проблем)

## ✅ Требования

### Система
- Python 3.8+
- SQL Server 2012+ или Azure SQL Database
- Доступ к серверу БД (IP, порт, учетные данные)

### Библиотеки
- `pymssql>=2.2.0` - драйвер для SQL Server

## 🔧 Установка зависимостей

### Автоматическая установка
```bash
pip install -r requirements.txt
```

### Ручная установка
```bash
pip install pymssql
```

### Проверка установки
```python
import pymssql
print(pymssql.__version__)
```

## 🔐 Настройка подключения

### Параметры подключения

При выборе источника данных "🗄️ SQL Server БД" в приложении, вам нужно указать:

| Параметр | Описание | Пример |
|----------|----------|---------|
| **Server (IP)** | IP адрес SQL Server | `192.168.1.100` |
| **Port** | Порт SQL Server | `1433` (по умолчанию) |
| **Database** | Название базы данных | `bdop` |
| **User** | Имя пользователя | `sales` |
| **Password** | Пароль | `your_password` |
| **Table** | Название таблицы | `Sales_table` |

### Пример конфигурации

```python
db_config = {
    'host': '192.168.1.100',
    'port': '1433',
    'database': 'bdop',
    'user': 'sales',
    'password': 'SecurePassword123',
    'table': 'Sales_table'
}
```

## 📊 Структура БД

### Требуемые колонки в таблице

Таблица должна содержать следующие колонки:

| Колонка SQL | Переименовывается в | Тип данных | Описание |
|-------------|---------------------|------------|----------|
| `shop` | `Magazin` | VARCHAR | Название магазина |
| `Datasales` | `Datasales` | DATETIME | Дата продажи |
| `Art` | `Art` | VARCHAR | Артикул товара |
| `Name_Product` | `Describe` | VARCHAR | Описание товара |
| `Model` | `Model` | VARCHAR | Модель товара |
| `Gender` | `Segment` | VARCHAR | Сегмент товара |
| `Cost_price` | `Purchaiseprice` | DECIMAL | Закупочная цена |
| `Price` | `Price` | DECIMAL | Цена продажи |
| `Qty` | `Qty` | INT | Количество |
| `[Sum]` | `Sum` | DECIMAL | Сумма продажи |

### SQL запрос

Приложение использует следующий SQL запрос:

```sql
SELECT TOP 100000
    shop as Magazin,
    Datasales,
    Art,
    Name_Product as Describe,
    Model,
    Gender as Segment,
    Cost_price as Purchaiseprice,
    Price,
    Qty,
    [Sum]
FROM [dbo].[Sales_table]
WHERE Datasales >= DATEADD(MONTH, -12, GETDATE())
    AND Qty > 0
ORDER BY Datasales DESC
```

**Особенности:**
- Загружаются данные за последние 12 месяцев
- Максимум 100,000 записей
- Только записи с количеством > 0
- Сортировка по дате (от новых к старым)

### Пример CREATE TABLE

```sql
CREATE TABLE [dbo].[Sales_table] (
    [id] INT IDENTITY(1,1) PRIMARY KEY,
    [shop] NVARCHAR(100) NOT NULL,
    [Datasales] DATETIME NOT NULL,
    [Art] NVARCHAR(50) NOT NULL,
    [Name_Product] NVARCHAR(255),
    [Model] NVARCHAR(100),
    [Gender] NVARCHAR(50),
    [Cost_price] DECIMAL(18, 2),
    [Price] DECIMAL(18, 2) NOT NULL,
    [Qty] INT NOT NULL,
    [Sum] DECIMAL(18, 2) NOT NULL
);

-- Индексы для производительности
CREATE INDEX IX_Datasales ON [dbo].[Sales_table](Datasales);
CREATE INDEX IX_Shop ON [dbo].[Sales_table](shop);
```

## 🚀 Использование

### Шаг 1: Выбор источника данных

В главном окне приложения выберите **"🗄️ SQL Server БД"**

### Шаг 2: Заполнение параметров подключения

Введите данные для подключения:
- Server (IP): `192.168.1.100`
- Port: `1433`
- Database: `bdop`
- User: `sales`
- Password: `***`
- Table: `Sales_table`

### Шаг 3: Подключение

Нажмите кнопку **"🔌 Подключиться к БД"**

### Шаг 4: Проверка данных

После успешного подключения:
- Появится сообщение: "✅ Загружено N записей из SQL Server"
- Доступен экспандер "🔍 Просмотр загруженных данных из БД"
- Проверьте первые 10 строк и типы данных

### Шаг 5: Работа с данными

Используйте все функции приложения:
- Прогнозирование продаж
- Аналитика
- ABC/XYZ анализ
- Анализ эластичности

## ⚠️ Решение проблем

### Проблема: "Модуль pymssql не установлен"

**Решение:**
```bash
pip install pymssql
```

### Проблема: "Ошибка авторизации: неверный логин/пароль"

**Причины:**
- Неверный логин или пароль
- Пользователь не существует
- Пользователь не имеет доступа к БД

**Решение:**
1. Проверьте учетные данные
2. Убедитесь, что пользователь создан в SQL Server
3. Предоставьте права на БД:
```sql
USE bdop;
GRANT SELECT ON [dbo].[Sales_table] TO sales;
```

### Проблема: "Сервер не найден. Проверьте IP и порт"

**Причины:**
- Неверный IP адрес
- Сервер недоступен
- Firewall блокирует подключение
- Неверный порт

**Решение:**
1. Проверьте IP адрес сервера: `ping 192.168.1.100`
2. Проверьте доступность порта: `telnet 192.168.1.100 1433`
3. Настройте Firewall для порта 1433
4. Включите TCP/IP в SQL Server Configuration Manager

### Проблема: "Превышено время ожидания подключения"

**Причины:**
- Медленная сеть
- Большая задержка до сервера
- Сервер перегружен

**Решение:**
1. Проверьте сетевое подключение
2. Увеличьте таймаут (в коде):
```python
# В файле src/utils/database_loader.py
conn = pymssql.connect(
    ...
    timeout=30,  # Увеличьте с 15 до 30
    login_timeout=30
)
```

### Проблема: "Отсутствуют обязательные колонки"

**Причины:**
- Таблица имеет другую структуру
- Неправильное название таблицы

**Решение:**
1. Проверьте структуру таблицы:
```sql
SELECT COLUMN_NAME, DATA_TYPE
FROM INFORMATION_SCHEMA.COLUMNS
WHERE TABLE_NAME = 'Sales_table';
```

2. Убедитесь, что все колонки присутствуют
3. При необходимости измените SQL запрос в `database_loader.py`

### Проблема: "Результат обрезан до 100,000 строк"

**Причины:**
- Слишком много данных в таблице

**Решение:**
1. Уменьшите период данных (измените в SQL запросе):
```sql
WHERE Datasales >= DATEADD(MONTH, -6, GETDATE())  -- Вместо -12
```

2. Увеличьте лимит (в коде):
```python
# В файле src/utils/database_loader.py
query = f"""
    SELECT TOP 200000  -- Увеличьте с 100000
    ...
```

## 🔒 Безопасность

### Рекомендации

1. **Не храните пароли в коде**
   - Используйте переменные окружения
   - Используйте .env файлы (не коммитьте в git!)

2. **Используйте минимальные привилегии**
   - Предоставляйте только SELECT права
   - Создавайте отдельного пользователя для приложения

3. **Защитите подключение**
   - Используйте VPN для удаленного доступа
   - Настройте Firewall для ограничения доступа

### Пример настройки пользователя

```sql
-- Создание пользователя с минимальными правами
CREATE LOGIN sales_app WITH PASSWORD = 'SecurePassword123';
USE bdop;
CREATE USER sales_app FOR LOGIN sales_app;
GRANT SELECT ON [dbo].[Sales_table] TO sales_app;
```

## 📈 Производительность

### Оптимизация запросов

1. **Индексы:**
```sql
CREATE INDEX IX_Datasales ON [dbo].[Sales_table](Datasales);
CREATE INDEX IX_Shop_Date ON [dbo].[Sales_table](shop, Datasales);
```

2. **Статистика:**
```sql
UPDATE STATISTICS [dbo].[Sales_table];
```

3. **Очистка старых данных:**
```sql
-- Удаление данных старше 2 лет
DELETE FROM [dbo].[Sales_table]
WHERE Datasales < DATEADD(YEAR, -2, GETDATE());
```

### Мониторинг

```sql
-- Проверка размера таблицы
SELECT
    t.NAME AS TableName,
    p.rows AS RowCounts,
    SUM(a.total_pages) * 8 AS TotalSpaceKB
FROM sys.tables t
INNER JOIN sys.partitions p ON t.object_id = p.object_id
INNER JOIN sys.allocation_units a ON p.partition_id = a.container_id
WHERE t.NAME = 'Sales_table'
GROUP BY t.Name, p.Rows;
```

## 💡 Советы

1. **Тестирование подключения:**
   - Сначала проверьте подключение через SQL Server Management Studio (SSMS)
   - Убедитесь, что SQL Server Authentication включена

2. **Кэширование:**
   - Данные из БД кэшируются с помощью `@st.cache_data`
   - При изменении данных в БД нужно перезапустить приложение

3. **Логирование:**
   - Проверяйте консоль Streamlit для отладки ошибок
   - При проблемах включите debug mode в SQL Server

## 📚 Дополнительные ресурсы

- [pymssql документация](https://pymssql.readthedocs.io/)
- [SQL Server документация](https://docs.microsoft.com/en-us/sql/sql-server/)
- [Streamlit документация](https://docs.streamlit.io/)

## ❓ FAQ

**Q: Можно ли использовать Windows Authentication?**

A: Да, но требуется другая настройка. Для Windows Authentication используйте:
```python
conn = pymssql.connect(
    server=host,
    database=database,
    trusted_connection=True
)
```

**Q: Поддерживается ли Azure SQL Database?**

A: Да, полностью поддерживается. Используйте формат:
- Server: `myserver.database.windows.net`
- Port: `1433`

**Q: Как изменить SQL запрос?**

A: Отредактируйте файл `src/utils/database_loader.py` в функции `_fetch_database_data()`

**Q: Можно ли подключиться к нескольким БД?**

A: В текущей версии - нет. Но можно легко доработать код для переключения между БД.

---

**Версия:** 1.0
**Дата:** 2024
